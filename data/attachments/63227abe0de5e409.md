# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: checkout-flow.spec.js >> Module 7 — Checkout Flow >> TC_CF_013 — Card form accepts dummy test card data
- Location: tests/checkout-flow.spec.js:227:7

# Error details

```
TimeoutError: locator.click: Timeout 15000ms exceeded.
Call log:
  - waiting for getByRole('button', { name: /continue/i }).first()

```

# Page snapshot

```yaml
- generic [ref=e3]:
  - banner [ref=e4]:
    - heading "Bad gateway Error code 502" [level=1] [ref=e5]:
      - generic [ref=e6]: Bad gateway
      - text: Error code 502
    - generic [ref=e7]:
      - text: Visit
      - link "cloudflare.com" [ref=e8]:
        - /url: https://www.cloudflare.com/5xx-error-landing?utm_source=errorcode_502&utm_campaign=in.accounts.ikea.com
      - text: for more information.
    - generic [ref=e9]: 2026-06-04 05:41:38 UTC
  - generic [ref=e12]:
    - generic [ref=e13]:
      - text: You
      - heading "Browser" [level=3] [ref=e17]
      - text: Working
    - generic [ref=e18]:
      - link [ref=e20]:
        - /url: https://www.cloudflare.com/5xx-error-landing?utm_source=errorcode_502&utm_campaign=in.accounts.ikea.com
      - text: San Jose
      - heading "Cloudflare" [level=3] [ref=e23]:
        - link "Cloudflare" [ref=e24]:
          - /url: https://www.cloudflare.com/5xx-error-landing?utm_source=errorcode_502&utm_campaign=in.accounts.ikea.com
      - text: Working
    - generic [ref=e25]:
      - text: in.accounts.ikea.com
      - heading "Host" [level=3] [ref=e29]
      - text: Error
  - generic [ref=e31]:
    - generic [ref=e32]:
      - heading "What happened?" [level=2] [ref=e33]
      - paragraph [ref=e34]: The web server reported a bad gateway error.
    - generic [ref=e35]:
      - heading "What can I do?" [level=2] [ref=e36]
      - paragraph [ref=e37]: Please try again in a few minutes.
  - paragraph [ref=e39]:
    - generic [ref=e40]:
      - text: "Cloudflare Ray ID:"
      - strong [ref=e41]: a0649c144e4ace44
    - text: •
    - generic [ref=e42]:
      - text: "Your IP:"
      - button "Click to reveal" [ref=e43] [cursor=pointer]
      - text: •
    - generic [ref=e44]:
      - text: Performance & security by
      - link "Cloudflare" [ref=e45]:
        - /url: https://www.cloudflare.com/5xx-error-landing?utm_source=errorcode_502&utm_campaign=in.accounts.ikea.com
```

# Test source

```ts
  1   | import { test } from '@playwright/test';
  2   | 
  3   | const PRODUCT_CARDS_SELECTOR = '.plp-product-compact, .pipf-product-compact, .pip-product-compact, .plp-fragment-wrapper, [data-testid="plp-product-card"], [data-testid="product-card"], .product-card, .item-card, .plp-product-card__container';
  4   | 
  5   | const FALLBACK_PRODUCTS = {
  6   |   sofas: '/in/en/p/glostad-3-seat-sofa-knisa-dark-grey-40595937/',
  7   |   beds: '/in/en/p/slattum-upholstered-bed-frame-vissle-dark-grey-60571247/',
  8   |   tables: '/in/en/p/micke-desk-white-30354274/',
  9   |   lighting: '/in/en/p/tagarp-floor-uplighter-black-white-40464050/',
  10  |   default: '/in/en/p/glostad-3-seat-sofa-knisa-dark-grey-40595937/'
  11  | };
  12  | 
  13  | /**
  14  |  * Shared helper utilities for IKEA India Playwright automation tests.
  15  |  * Handles cookie consent, popups, login, and common navigation tasks.
  16  |  */
  17  | 
  18  | /**
  19  |  * Dismisses the IKEA cookie consent banner and any location/country popups.
  20  |  * Called in beforeEach of every test to keep tests clean.
  21  |  * @param {import('@playwright/test').Page} page
  22  |  */
  23  | export async function dismissCookieAndPopups(page) {
  24  |   // Dismiss IKEA's OneTrust cookie consent banner (button text is "Ok")
  25  |   try {
  26  |     const cookieBtn = page.locator('#onetrust-accept-btn-handler');
  27  |     await cookieBtn.click({ timeout: 5000 });
  28  |   } catch {
  29  |     // Cookie banner may not appear — safe to ignore
  30  |   }
  31  | 
  32  |   // Bypass the corporate landing page (yellow "Go shopping" button)
  33  |   try {
  34  |     const goShoppingBtn = page.locator('text="Go shopping"').first();
  35  |     if (await goShoppingBtn.isVisible({ timeout: 3000 })) {
  36  |       await goShoppingBtn.click();
  37  |       await page.waitForLoadState('domcontentloaded'); await page.waitForTimeout(3000);
  38  |     }
  39  |   } catch {
  40  |     // Already on the shopping page
  41  |   }
  42  | 
  43  |   // Dismiss any location/country popup
  44  |   try {
  45  |     const closePopup = page.locator('[data-testid="popup-close"], [aria-label="Close"], button:has-text("Close"), .modal-close');
  46  |     await closePopup.first().click({ timeout: 3000 });
  47  |   } catch {
  48  |     // No popup — safe to ignore
  49  |   }
  50  | 
  51  |   // Dismiss any promotional overlay or notification
  52  |   try {
  53  |     const promoClose = page.locator('[data-testid="promo-close"], .notification-close, [aria-label="Dismiss"]');
  54  |     await promoClose.first().click({ timeout: 2000 });
  55  |   } catch {
  56  |     // No promo — safe to ignore
  57  |   }
  58  | }
  59  | 
  60  | /**
  61  |  * Performs login with the pre-created test account.
  62  |  * Uses credentials from environment variables.
  63  |  * @param {import('@playwright/test').Page} page
  64  |  */
  65  | export async function loginWithTestAccount(page) {
  66  |   const email = process.env.TEST_EMAIL || 'testuser@example.com';
  67  |   const password = process.env.TEST_PASSWORD || 'Test@12345';
  68  | 
  69  |   await page.goto('/in/en/profile/login/');
  70  |   await handleTurnstileGracefully(page);
  71  |   await dismissCookieAndPopups(page);
  72  | 
  73  |   const continueBtn = page.getByRole('button', { name: /continue/i }).first();
  74  |   await continueBtn.waitFor({ state: 'visible', timeout: 5000 }).catch(() => {});
  75  | 
  76  |   // Fill login form
  77  |   await page.getByLabel(/email/i).fill(email).catch(() => {});
  78  |   await page.getByLabel(/password/i).fill(password).catch(() => {});
  79  | 
  80  |   await continueBtn.scrollIntoViewIfNeeded().catch(() => {});
> 81  |   await continueBtn.click({ force: true });
      |                     ^ TimeoutError: locator.click: Timeout 15000ms exceeded.
  82  |   await page.waitForTimeout(1000);
  83  |   await handleTurnstileGracefully(page);
  84  | 
  85  |   // Wait for navigation after login
  86  |   await page.waitForLoadState('domcontentloaded').catch(() => {});
  87  |   await page.waitForTimeout(3000);
  88  | }
  89  | 
  90  | /**
  91  |  * Adds a product to the cart from a product listing or search results page.
  92  |  * Navigates to a known product category and adds the first available item.
  93  |  * @param {import('@playwright/test').Page} page
  94  |  * @param {string} [category] - Category URL path segment (default: sofas)
  95  |  */
  96  | export async function addProductToCart(page, category = 'cat/sofas-fu003/') {
  97  |   // Build proper category URL path
  98  |   const cleanCategory = category.startsWith('/') ? category : `/${category}`;
  99  |   const categoryPath = cleanCategory.startsWith('/in/en/') ? cleanCategory : `/in/en/${category.replace(/^\//, '')}`;
  100 |   
  101 |   console.log(`Navigating to category: ${categoryPath}`);
  102 |   try {
  103 |     await page.goto(categoryPath, { timeout: 15000, waitUntil: 'domcontentloaded' });
  104 |   } catch (e) {
  105 |     if (e.name === 'TimeoutError' || e.message.includes('timeout') || e.message.includes('navigation')) {
  106 |       test.skip(true, `Category page navigation timed out or was blocked by bot challenge: ${categoryPath}`);
  107 |     } else {
  108 |       throw e;
  109 |     }
  110 |   }
  111 |   await handleTurnstileGracefully(page);
  112 |   await dismissCookieAndPopups(page);
  113 |   await page.waitForLoadState('domcontentloaded').catch(() => {});
  114 |   await page.waitForTimeout(3000);
  115 |   
  116 |   let navigated = false;
  117 |   const cards = page.locator(PRODUCT_CARDS_SELECTOR);
  118 |   if (await cards.first().isVisible({ timeout: 5000 })) {
  119 |     const link = cards.first().locator('a[href*="/p/"]').first();
  120 |     if (await link.isVisible({ timeout: 2000 })) {
  121 |       await link.click({ timeout: 5000 }).catch(async () => {
  122 |         await link.click({ force: true }).catch(() => {});
  123 |       });
  124 |       navigated = true;
  125 |     } else {
  126 |       await cards.first().click({ timeout: 5000 }).catch(async () => {
  127 |         await cards.first().click({ force: true }).catch(() => {});
  128 |       });
  129 |       navigated = true;
  130 |     }
  131 |     await page.waitForTimeout(4000);
  132 |   }
  133 |   
  134 |   // If card click didn't land on detail page, go directly to our active stable product fallback
  135 |   if (!navigated || !page.url().includes('/p/')) {
  136 |     let fallbackUrl = FALLBACK_PRODUCTS.default;
  137 |     const lowerCat = category.toLowerCase();
  138 |     if (lowerCat.includes('bed')) fallbackUrl = FALLBACK_PRODUCTS.beds;
  139 |     else if (lowerCat.includes('table') || lowerCat.includes('desk')) fallbackUrl = FALLBACK_PRODUCTS.tables;
  140 |     else if (lowerCat.includes('light') || lowerCat.includes('lamp')) fallbackUrl = FALLBACK_PRODUCTS.lighting;
  141 |     else if (lowerCat.includes('sofa')) fallbackUrl = FALLBACK_PRODUCTS.sofas;
  142 |     
  143 |     console.log(`Prerequisite category card click did not navigate. Direct routing to stable fallback: ${fallbackUrl}`);
  144 |     try {
  145 |       await page.goto(fallbackUrl, { timeout: 15000, waitUntil: 'domcontentloaded' });
  146 |     } catch (e) {
  147 |       if (e.name === 'TimeoutError' || e.message.includes('timeout') || e.message.includes('navigation')) {
  148 |         test.skip(true, `Fallback product page navigation timed out or was blocked by bot challenge: ${fallbackUrl}`);
  149 |       } else {
  150 |         throw e;
  151 |       }
  152 |     }
  153 |     await handleTurnstileGracefully(page);
  154 |     await dismissCookieAndPopups(page);
  155 |     await page.waitForLoadState('domcontentloaded').catch(() => {});
  156 |     await page.waitForTimeout(3000);
  157 |   }
  158 |   
  159 |   // Click Add to Bag
  160 |   const addBtn = page.getByRole('button', { name: /add to bag|add to cart/i }).first();
  161 |   await addBtn.scrollIntoViewIfNeeded().catch(() => {});
  162 |   await addBtn.click({ force: true }).catch(() => addBtn.evaluate(el => el.click()));
  163 |   
  164 |   // Wait for cart update animation
  165 |   await page.waitForTimeout(4000);
  166 |   
  167 |   // Dismiss side drawer toast if visible
  168 |   const closeBtn = page.locator('[data-testid="toast-close"], button[aria-label*="close" i], .hn-popup-close-btn, .modal-close').first();
  169 |   if (await closeBtn.isVisible({ timeout: 2000 })) {
  170 |     await closeBtn.click().catch(() => {});
  171 |     await page.waitForTimeout(1000);
  172 |   }
  173 | }
  174 | 
  175 | /**
  176 |  * Gets the current cart badge count from the header.
  177 |  * @param {import('@playwright/test').Page} page
  178 |  * @returns {Promise<number>} The cart item count
  179 |  */
  180 | export async function getCartBadgeCount(page) {
  181 |   try {
```